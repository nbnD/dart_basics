void main() {
  int a = 10;
  var b = 6;

  var sum = a + b;
  var diff = a - b;
  var mul = a * b;
  var div = a / b;
  var mod = a % b;

  // print("Sum:$sum\nDiff:$diff\nMul: $mul\nDiv:$div\nRemainder is $mod");

  const PI = 3.14;
  var radius = 12;

  var area = PI * radius * radius;

  print("Area of the circle is ${area.toStringAsFixed(3)}");
}
